sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t priya/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true priya/react-test npm test

after_success:
  - docker build -t priya/multi-client ./client
  - docker build -t priya/multi-nginx ./nginx
  - docker build -t priya/multi-server ./server
  - docker build -t priya/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push priya/multi-client
  - docker push priya/multi-nginx
  - docker push priya/multi-server
  - docker push priya/multi-worker

deploy:
  provider: elasticbeanstalk
  region: 'us-east-1'
  app: 'multi-docker'
  env: 'MultiDocker-env'
  bucket_name: 'elasticbeanstalk-us-east-1-923445559289'
  bucket_path: 'docker-multi'
  on:
    branch: main
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
